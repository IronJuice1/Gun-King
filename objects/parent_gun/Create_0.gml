/// @desc

//Make the object a singleton
if (assign_singleton() == false)	exit

// Default values
cooldown_timer_max = 100
cooldown_timer = cooldown_timer_max

shake_duration = 0
shake_duration_max = 40 
shake_factor_each_duration_tick = 0.1
