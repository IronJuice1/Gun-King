/// @desc

if instance_exists(parent_player) {

	x_to = parent_player.x + x_offset
	y_to = parent_player.y + y_offset

	x += (x_to - x)/response	
	y += (y_to - y)/response
	
	//Move a flat amout a little?
	
	
}


